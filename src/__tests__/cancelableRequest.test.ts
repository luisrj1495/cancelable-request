describe("cancelableRequest", () => {
  test.todo(
    "Ensure we cancel a request if we execute the function at the same time"
  );

  test.todo("Ensure we cancel a request using the cancel function");
});
